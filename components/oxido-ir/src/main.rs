//mod input_file;
//mod span;
// mod token;
//mod token_tree;
//mod word;

// use input_file::InputFile;
// use span::LineColumn;
// use token::Token;
//use word::Word;

//use crate::{span::Span, token_tree::TokenTree};

fn main() {
    /*let i = InputFile {
      name: Word{string: "test.dada".to_owned()},
      source_text: String::from("value"),
      breakpoint_locations: vec![
        LineColumn::new0(1u32, 1u32)
      ]
    };
    let tt = TokenTree {
      input_file: i,
      span: Span::from(1, 4),
      tokens: vec![Token::Op('+'), Token::Op('-')],
    };*/
    /*let w = Word {
        string: "test.dada".to_owned(),
    };*/

    //assert_eq!("test.dada", w.as_str());
    //println!("{}", w.as_str() == "test.dada")
    //println!("{w:#?}");
}
